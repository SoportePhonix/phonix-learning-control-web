export type UserFormValues = {
  name: string;
  lastName: string;
  typeOfIdentificationDocument?: string;
  identificationDocument: string;
  email: string;
  password?: string;
  roleId?: string;
};
